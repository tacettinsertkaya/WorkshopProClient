import { UserDirectiveDirective } from './user-directive.directive';

describe('UserDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new UserDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
