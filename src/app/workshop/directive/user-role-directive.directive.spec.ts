import { UserRoleDirectiveDirective } from './user-role-directive.directive';

describe('UserRoleDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new UserRoleDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
